Data from music streaming company that also includes music data, customer and employee data
link to diagram: https://dbdiagram.io/d/chinook-bit-684a02a5a463a450da2640c1


--Show Customers (their full names, customer ID, and country) who are not in the US.
SELECT
    CustomerId,
    FirstName || ' ' || LastName AS FullName,
    Country
FROM
    Customers
WHERE
    Country <> 'USA';

--Show only the Customers from Brazil.
SELECT
    CustomerId,
    FirstName || ' ' || LastName AS FullName,
    Country
FROM Customers
WHERE Country = 'Brazil';

--Find the Invoices of customers who are from Brazil. The resulting table should show the customer's full name, Invoice ID, Date of the invoice, and billing country.
SELECT
    FirstName || ' ' || LastName AS FullName,
    InvoiceId,
    InvoiceDate,
    BillingCountry
FROM customers
JOIN invoices
    ON customers.CustomerId = invoices.CustomerId
WHERE Country = 'Brazil';

--Show the Employees who are Sales Agents.
SELECT 
EmployeeId, 
FirstName,
LastName,
Title
FROM employees
WHERE Title = 'Sales Support Agent';

--Find a unique/distinct list of billing countries from the Invoice table.
SELECT
    FirstName || ' ' || LastName AS FullName,
    InvoiceId,
    InvoiceDate,
    BillingCountry
FROM customers 
INNER JOIN invoices 
    ON InvoiceId = InvoiceId
WHERE Country = 'Brazil';

--Show the Employees who are Sales Agents.
SELECT
    EmployeeId,
    FirstName || ' ' || LastName AS FullName,
    Title
FROM employees
WHERE Title = 'Sales Support Agent';

--Find a unique/distinct list of billing countries from the Invoice table.
SELECT DISTINCT
    BillingCountry
FROM invoices
ORDER BY BillingCountry;

--Provide a query that shows the invoices associated with each sales agent. The resulting table should include the Sales Agent's full name.
[In this case I had to use aliases to cut down on any confusion on which tables are what. C = customers, E = Employees, I = Invoices]
SELECT
    e.FirstName || ' ' || e.LastName AS SalesAgentFullName,
    i.InvoiceId,
    i.InvoiceDate,
    i.BillingCountry,
    i.Total
FROM employees e
INNER JOIN customers c
    ON e.EmployeeId = c.SupportRepId
INNER JOIN invoices i
    ON c.CustomerId = i.CustomerId
WHERE e.Title = 'Sales Support Agent'
ORDER BY SalesAgentFullName, i.InvoiceDate;

--Show the Invoice Total, Customer name, Country, and Sales Agent name for all invoices and customers.
SELECT
    i.Total AS InvoiceTotal,
    c.FirstName || ' ' || c.LastName AS CustomerName,
    c.Country,
    e.FirstName || ' ' || e.LastName AS SalesAgentName
FROM invoices i
INNER JOIN customers c
    ON i.CustomerId = c.CustomerId
INNER JOIN employees e
    ON c.SupportRepId = e.EmployeeId
ORDER BY i.InvoiceId;

--How many Invoices were there in 2009?
SELECT
    COUNT(*) AS TotalInvoices_2009
FROM invoices
WHERE strftime('%Y', InvoiceDate) = '2009';

--What are the total sales for 2009? [used strftime('%Y, InvoiceDate) to express the year I wanted the result] 
SELECT
    SUM(Total) AS TotalSales_2009
FROM invoices
WHERE strftime('%Y', InvoiceDate) = '2009';

--Write a query that includes the purchased track name with each invoice line ID. [ii = invoice items]
SELECT
    ii.InvoiceLineId,
    t.Name AS TrackName
FROM invoice_items ii
INNER JOIN tracks t
    ON ii.TrackId = t.TrackId
ORDER BY ii.InvoiceLineId;

--Write a query that includes the purchased track name AND artist name with each invoice line ID.
SELECT
    ii.InvoiceLineId,
    t.Name AS TrackName,
    ar.Name AS ArtistName
FROM invoice_items ii
INNER JOIN tracks t
    ON ii.TrackId = t.TrackId
INNER JOIN albums al
    ON t.AlbumId = al.AlbumId
INNER JOIN artists ar
    ON al.ArtistId = ar.ArtistId
ORDER BY ii.InvoiceLineId;

--Provide a query that shows all the Tracks, and include the Album name, Media type, and Genre.
SELECT
    t.Name AS TrackName,
    al.Title AS AlbumName,
    mt.Name AS MediaType,
    g.Name AS Genre
FROM tracks t
INNER JOIN albums al
    ON t.AlbumId = al.AlbumId
INNER JOIN media_types mt
    ON t.MediaTypeId = mt.MediaTypeId
INNER JOIN genres g
    ON t.GenreId = g.GenreId
ORDER BY t.Name;

--Show the total sales made by each sales agent.
SELECT
    e.FirstName || ' ' || e.LastName AS SalesAgentName,
    SUM(i.Total) AS TotalSales
FROM employees e
INNER JOIN customers c
    ON e.EmployeeId = c.SupportRepId
INNER JOIN invoices i
    ON c.CustomerId = i.CustomerId
WHERE e.Title = 'Sales Support Agent'
GROUP BY e.EmployeeId
ORDER BY TotalSales DESC;

--Which sales agent made the most dollars in sales in 2009?
SELECT
    e.FirstName || ' ' || e.LastName AS SalesAgentName,
    SUM(i.Total) AS TotalSales_2009
FROM employees e
INNER JOIN customers c
    ON e.EmployeeId = c.SupportRepId
INNER JOIN invoices i
    ON c.CustomerId = i.CustomerId
WHERE e.Title = 'Sales Support Agent'
  AND strftime('%Y', i.InvoiceDate) = '2009'
GROUP BY e.EmployeeId
ORDER BY TotalSales_2009 DESC
LIMIT 1;

